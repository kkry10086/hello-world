

## fork：

linux使用fork时是读时共享，写时拷贝。
当父子进程对于同一数据在只有读时，会共享同一个数据。这样可以将fork复制子进程时需要的内存空间的浪费减少。
而当父子进程有任一进程对数据进行了写操作，那么这时才会将数据拷贝到子进程里面。此时所有进程都有自己的进程。

让父子进程共享同一个地址空间，只有在需要写入的时候才会复制地址空间，从而使各个进程拥有自己的地址空间。

父子进程共享文件，fork产生的子进程与父进程相同的文件描述符指向相同的文件表。