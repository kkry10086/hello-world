OBJS=add.o sub.o test.o

#$@表示目标
#$^表示所有的依赖
#$<表示第一个依赖


#模式匹配 所有的.o都依赖相应的.c
#将所有的.c生成对应的.o
%.o:%.c
	g++ -c $< -o $@

test:$(OBJS)
	g++ $^ -o $@

#add.o:add.c
#	g++ -c $< -o $@

#sub.o:sub.c
#	g++ -c $< -o $@

#test.o:test.c
#	g++ -c $< -o $@

clean:
	rm $(OBJS) test
