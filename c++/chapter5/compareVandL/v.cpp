#include<iostream>

// #include"student.h"
#include<string>
#include<fstream>


using std::cin;using std::cout;
using std::endl;using std::ofstream;
using std::string;using std::getline;


int main(){
  //  vector<Student_info> v;
  //Student_info record;
  string::size_type maxlen=0;

  string filename="xinxi",s;
  
  ofstream fs(filename);
  
  while(!fs.eof()){

    cout<<s<<endl;
  }
  fs.close();

  
}
